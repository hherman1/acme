#!/bin/sh
set -e
plumb $HOME/.config/notes/todo
WINID=$(9p read acme/index | grep '/.config/notes/todo Del Snarf' | awk '{print $1}')
acmectl ctl $WINID cleartag
printf "+P |t+ |t- 'note sync'" | acmectl write $WINID tag
